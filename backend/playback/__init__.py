"""Playback layer (HARDWARE-TIERS §2.6).

HQPlayer-specific connection management (`hqp_backend`), the
source-agnostic play tracker (`tracker`) and listening-session archival
(`sessions`) live here; `routers/player.py` keeps the HTTP surface.
"""
