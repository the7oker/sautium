# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Database
psycopg2-binary>=2.9.9
pgvector>=0.2.4
sqlalchemy>=2.0.25

# Audio Processing
mutagen>=1.47.0
soundfile>=0.12.1
audioread>=3.0.1
# Built-in player output (WASAPI/ASIO/CoreAudio via PortAudio). The wheel
# bundles PortAudio on Windows/macOS; on Linux (Docker) the import guard in
# playback/local/devices.py degrades to zero devices.
sounddevice>=0.5.1
# DLNA output: SSDP discovery + AVTransport/RenderingControl + GENA eventing
# (Home Assistant's UPnP stack, rides the aiohttp already in the deps).
async-upnp-client>=0.36.0

# Image Processing (cover art)
Pillow>=10.2.0
imagehash>=4.3.1

# Machine Learning
# Pinned: torch._dynamo (imported by transformers at model load) is sensitive
# to the torch×Python stdlib pair. Unpinned `>=` previously drifted to a torch
# whose _dynamo polyfills require stdlib symbols absent from the image's Python,
# breaking every embedding model load. This is the verified GPU-working set.
#
# The marker excludes Intel macOS, where the local-analysis stack cannot be
# installed at all: PyTorch stopped publishing wheels after 2.2.2, and
# librosa's numba/llvmlite chain has no wheel either and fails to build from
# source. An unsatisfiable requirement fails the WHOLE `pip install -r`, so
# leaving these unmarked left such a node without fastapi either.
#
# Nothing is lost by dropping them there. Those machines have neither CUDA
# nor MPS, so hardware_profile resolves `lite` — no local analysis, no
# pre-warm, no translation; analysis arrives over P2P. Every module that
# imports torch or librosa sits on an analysis path gated by that profile.
# Anything that imports either needs the same marker, or it drags the
# unsatisfiable pin back in as a transitive dependency.
torch==2.12.0; sys_platform != "darwin" or platform_machine != "x86_64"
torchvision==0.27.0; sys_platform != "darwin" or platform_machine != "x86_64"
torchaudio==2.11.0; sys_platform != "darwin" or platform_machine != "x86_64"
transformers==4.57.6
sentence-transformers==5.5.1; sys_platform != "darwin" or platform_machine != "x86_64"
hear21passt>=0.0.26; sys_platform != "darwin" or platform_machine != "x86_64"
librosa>=0.10.1; sys_platform != "darwin" or platform_machine != "x86_64"
# MADLAD-400 int8 CPU runtime (query translation; future chat/voice synergy)
ctranslate2>=4.5.0

# AI/LLM
anthropic>=0.79.0
openai>=1.0.0
# SDK 2.0 removed mcp.server.fastmcp (FastMCP left the SDK) — the assistant
# MCP server targets the 1.x API; unpinned installs silently broke its spawn.
mcp>=1.2,<2

# External APIs
pylast>=5.2.0
lyricsgenius>=3.0.0

# P2P Identity
argon2-cffi>=23.1.0
cryptography>=41.0.0

# Utilities
python-dotenv>=1.0.0
psutil>=5.9.0
click>=8.1.7
tqdm>=4.66.1
numpy>=1.26.3
pandas>=2.2.0
scipy>=1.2.0

# HTTP Client
httpx>=0.26.0
requests>=2.31.0
aiohttp>=3.9.1

# Logging
python-json-logger>=2.0.7

# Development
pytest>=7.4.4
pytest-asyncio>=0.23.3
Unidecode>=1.3.8

# Streaming preview — phantom-album resolution (non-DRM; ffmpeg already in image)
yt-dlp>=2024.12.0

# Cross-script search transliteration (Phase 0a) — name_latin population.
# All permissive (anyascii ISC, rest MIT) and ALL ship binary wheels, so the
# launcher's --only-binary=:all: install succeeds. unidic-lite (cutlet's Japanese
# dictionary) is sdist-only — NO wheel — so it must NOT go here or --only-binary
# fails the entire install; the Docker image installs it separately (Dockerfile),
# and transliterate._japanese degrades to anyascii where it's absent.
anyascii>=0.3.3
pypinyin>=0.55.0
cutlet>=0.5.0
koroman>=1.0.16
