# Upper bound on purpose: every launcher screen is verified against 5.2, and
# a fresh install is the one place a major bump would land untested.
customtkinter>=5.2.0,<6
pystray>=0.19.5
Pillow>=10.0.0
qrcode>=7.4.0
psutil>=6.0.0
psycopg2-binary>=2.9.9
cryptography>=41.0.0
aiohttp>=3.9.1
libtorrent>=2.0.0
argon2-cffi>=23.1.0
PyNaCl>=1.5.0
